{
  "name": "chat-app",
  "private": true,
  "scripts": {
    "postinstall": "cd client && npm install && npm run build && cd .. && cd server && npm install",
    "start": "cd server && node src/index.js"
  },
  "dependencies": {}
}
