{
  "name": "chat-app",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "npm run build && cd server && npm install && node src/index.js",
    "dev": "concurrently \"cd client && npm run dev\" \"cd server && npm run dev\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
